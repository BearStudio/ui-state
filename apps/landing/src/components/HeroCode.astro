---
// One sample: incomplete match + the tsc miss. No getUiState dump.
---

<pre
  class="overflow-x-auto rounded-lg border border-line bg-code p-5 font-mono text-[13px] leading-6 text-paper sm:text-sm"
><code>ui.<span class="text-code-fn">match</span>(<span class="text-code-str">"pending"</span>, () => &lt;Loading /&gt;)
  .<span class="text-code-fn">match</span>(<span class="text-code-str">"error"</span>, () => &lt;Error /&gt;)
  .<span class="text-code-fn">match</span>(<span class="text-code-str">"default"</span>, (&#123; book &#125;) => &lt;Book book=&#123;book&#125; /&gt;)
  .<span class="text-code-fn">exhaustive</span>()
        <span class="text-accent">~~~~~~~~~~</span>

<span class="text-code-muted">error TS2349</span>
Type <span class="text-code-str">'Unmatched&lt;"empty"&gt;'</span> has no call signatures.</code></pre>
