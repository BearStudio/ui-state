---
// The sell: exhaustive() is a type error until every status is matched.
---

<figure>
  <figcaption class="mb-3 font-mono text-xs text-accent">
    tsc
  </figcaption>
  <pre
    class="overflow-x-auto rounded-lg border border-line bg-code p-5 font-mono text-[13px] leading-6 text-paper"
  ><code><span class="text-code-muted">book-screen.tsx:14:6 - error TS2349</span>
This expression is not callable.
  Type <span class="text-code-str">'Unmatched&lt;"empty"&gt;'</span> has no call signatures.

  12   .match(<span class="text-code-str">"default"</span>, (&#123; book &#125;) =&gt; &lt;Book book=&#123;book&#125; /&gt;)
  13   .match(<span class="text-code-str">"error"</span>, () =&gt; &lt;Error /&gt;)
  14   .exhaustive()
        <span class="text-accent">~~~~~~~~~~</span></code></pre>
</figure>
