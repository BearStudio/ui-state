---
// Nested ternaries still typecheck when empty is missing.
---

<pre
  class="overflow-x-auto rounded-lg border border-line bg-paper p-5 font-mono text-[13px] leading-6 text-ink-soft sm:text-sm"
><code><span class="text-ink">return</span> isPending ? (
  &lt;Loading /&gt;
) : isError ? (
  &lt;Error /&gt;
) : data ? (
  &lt;Book book=&#123;data&#125; /&gt;
) : <span class="text-ink">null</span>;</code></pre>
